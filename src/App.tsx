import React, { FC } from 'react';
import { MainPage } from './pages';

const App: FC = (): JSX.Element => {
  ///TODO ZMiana języka, filtrowanie  wszystkie/Ukonczone/aktywne 404 NotFound z Lazy loading
  return <MainPage />;
};

export default App;
